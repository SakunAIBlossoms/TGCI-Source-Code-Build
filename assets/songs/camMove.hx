
/*
function update() {
    camGame.angle = FlxMath.lerp(camGame.angle, 0, 0.2);
    //camGame.scroll.x = FlxMath.lerp(camGame.x, camFollow.x, 0.2);
    //camGame.scroll.y = FlxMath.lerp(camGame.y, camFollow.y, 0.2);
}
function onNoteHit(event) {
    switch(event.direction) {
        case 0:
            camGame.scroll.x -= 1;
            camGame.angle -= 1;
        case 1:
            camGame.scroll.y -= 1;
        case 2:
            camGame.scroll.y += 1;
        case 3:
            camGame.scroll.x += 1;
            camGame.angle += 1;
    }
}
    */