function create() {
	enableCameraHacks = false;
}

function postCreate() {
	importScript("data/scripts/fakedesktop");
}

function scaryKYSguy() {
	trace("spawn the funny moving guys if it crashes haxe fucking sucks ass");
}
function traceshit() {
	trace("traced something");
}
function postUpdate(elapsed) {
	

	//for(s in strumLines) {
	//	for(i in 0...4) {
	//		var n = s.members[i];
	//		n.angle = Math.sin(curBeatFloat + (i * 0.45)) * 35;
	//	}
	//}


	// for(s in strumLines) {
	// 	for(i in 0...4) {
	// 		var n = s.members[i];
	// 		n.y = FlxG.height - 200;
	// 		n.angle = 180;
	// 	}
	// }

	// if (curSection != null)
	//     defaultCamZoom = curSection.mustHitSection ? 0.9 : 0.5;
}